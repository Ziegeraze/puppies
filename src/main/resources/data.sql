-- Spring Boot requires a non-empty data.sql if the file exists
-- You can add initial data here if needed for your application

-- Adding a placeholder statement to ensure the script is not empty
CREATE TABLE IF NOT EXISTS placeholder_table (id INT);
